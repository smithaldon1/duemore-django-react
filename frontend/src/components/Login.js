import React from 'react';
import styled from 'styled-components';

const LoginWrapper = styled.div`
    margin-top: 7rem;
`;

export const Login = () => {
    return (
        <LoginWrapper>
            <h1>Login Page</h1>
        </LoginWrapper>
    )
}
